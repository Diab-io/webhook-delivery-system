# webhook-delivery-system
Webhook delivery API that lets users register webhooks for specific events. When an event is triggered, all subscribed endpoints automatically receive the corresponding payload.
